# Dependencies & build output
node_modules/
build/
dist/
coverage/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Local AI Studio / scratch folders — not part of the public source
assets/

# Package-manager lockfile drift (npm is canonical) — kept on disk, not tracked
bun.lock

# Temporary PDF text-extraction export — kept on disk, not tracked
pdf-forge-exports/

# Legacy project-history archive — large binaries kept on disk, not tracked.
# The inventory is documented in PROJECT_HISTORY/RELEASE_HISTORY_ASSETS.md
PROJECT_HISTORY/ARCHIVES/

# Secrets & local config — NEVER commit real values
.env
.env.*
!.env.example
