# Dependencies & build output
node_modules/
build/
dist/
coverage/

# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Local AI Studio / scratch folders — not part of the public source.
# Anchored to the repo root so nested assets/ (e.g. WEBSITE_v1.1/src/assets/) stays tracked.
/assets/

# Package-manager lockfile drift (npm is canonical) — kept on disk, not tracked
bun.lock

# Temporary PDF text-extraction export — kept on disk, not tracked
pdf-forge-exports/

# Legacy project-history archive — large binaries kept on disk, not tracked.
# The inventory is documented in PROJECT_HISTORY/RELEASE_HISTORY_ASSETS.md
BRAND/PROJECT_HISTORY/ARCHIVES/

# External skill bundles — never tracked (kept on disk for local agent use)
.opencode/skills/ecc-harness/
.opencode/skills/emilkowalski-skills/
.opencode/skills/ui-ux-pro-max/

# Secrets & local config — NEVER commit real values
.env
.env.*
!.env.example
